# Artefactos de colcon (se regeneran con `colcon build`)
build/
install/
log/

# Python
__pycache__/
*.pyc

# Editor
.vscode/

# Claude Code local config (agentes, etc.) -- se queda solo en esta PC
.claude/
